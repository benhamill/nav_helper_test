= content_for(:main) do
  .page-header
    h1
      | Great App
      - if content_for?(:subhead)
        small= yield(:subhead)
  .row
    .span3
      ul.sub.nav.nav-tabs.nav-stacked
        li.settings= link_to 'Settings', great_app_settings_path

    .span9
      = yield

= render template: 'layouts/application'
